# The report defines the exponential count term but does not prescribe lambda.
conflict_decay_lambda: 0.5
roles:
  administrator: [administrator, admin]
  owner: [owner]
  driver: [driver]
direct_interface_policy:
  enabled: false
  requires_authentication: true
