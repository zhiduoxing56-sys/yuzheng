# 千问 3B 自然语言语义帧抽取最小测试结果

- 模型：`qwen2.5:3b-instruct-q4_0`
- 预热次数：3
- 每条执行次数：3
- 初步判断：待人工确认

## 固定系统提示词

```text
这是车辆控制语义抽取任务。
一句话可能包含一个或多个独立子意图。
每个子意图只能抽取四个字段：动作、对象、区域、数值。
没有明确区域时填 null；没有明确数值时填 null。
不得自行补充用户没有表达的信息。
多个子意图必须保持原始指令中的出现顺序。
只能输出合法 JSON，格式必须是：{"子意图":[{"动作":null,"对象":null,"区域":null,"数值":null}]}。
不得在 JSON 前后附加任何说明。
```

## 输入
打开右前门

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"打开","对象":"右前门"},{"动作":null,"对象":null,"区域":null,"数值":null}]}
```

首字节等待：488.856 ms；完整返回：848.561 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"打开","对象":"右前门"}]}
```

首字节等待：488.88 ms；完整返回：661.329 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"打开","对象":"右前门"}]}
```

首字节等待：519.721 ms；完整返回：690.581 ms；合法 JSON：True

初步判断：待人工确认

## 输入
关闭左前车窗

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"关闭","对象":"车窗","区域":"左前"}]}
```

首字节等待：519.657 ms；完整返回：781.001 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"关闭","对象":"车窗","区域":"左前"}]}
```

首字节等待：451.144 ms；完整返回：673.134 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"关闭","对象":"车窗","区域":"左前"}]}
```

首字节等待：472.483 ms；完整返回：705.401 ms；合法 JSON：True

初步判断：待人工确认

## 输入
把左后车窗开到一半

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"开启","对象":"车窗","区域":"左后","数值":"一半"}]}
```

首字节等待：505.778 ms；完整返回：773.314 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"开启","对象":"车窗","区域":"左后","数值":"一半"}]}
```

首字节等待：509.297 ms；完整返回：765.925 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"开启","对象":"车窗","区域":"左后","数值":"一半"}]}
```

首字节等待：455.131 ms；完整返回：713.792 ms；合法 JSON：True

初步判断：待人工确认

## 输入
副驾驶的窗户打开一点

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"开启","对象":"窗户","区域":"副驾驶","数值":"一点"}]}
```

首字节等待：527.171 ms；完整返回：808.204 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"开启","对象":"窗户","区域":"副驾驶","数值":"一点"}]}
```

首字节等待：482.575 ms；完整返回：727.826 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"开启","对象":"窗户","区域":"副驾驶","数值":"一点"}]}
```

首字节等待：535.715 ms；完整返回：781.848 ms；合法 JSON：True

初步判断：待人工确认

## 输入
把我右边的车门打开

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"打开","对象":"车门","区域":"右边","数值":null}]}
```

首字节等待：510.111 ms；完整返回：761.259 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"打开","对象":"车门","区域":"右边"}]}
```

首字节等待：541.541 ms；完整返回：742.34 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"打开","对象":"车门","区域":"右边"}]}
```

首字节等待：519.524 ms；完整返回：740.025 ms；合法 JSON：True

初步判断：待人工确认

## 输入
后排左手边那个玻璃降下来

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"打开","对象":"玻璃","区域":"后排左手边"},{"动作":null,"对象":null,"区域":null,"数值":null}]}
```

首字节等待：565.264 ms；完整返回：955.712 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"打开","对象":"玻璃","区域":"后排左手边"},{"动作":null,"对象":null,"区域":null}]}
```

首字节等待：485.423 ms；完整返回：848.039 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"打开","对象":"玻璃","区域":"后排左手边"},{"动作":null,"对象":null,"区域":null}]}
```

首字节等待：501.999 ms；完整返回：871.806 ms；合法 JSON：True

初步判断：待人工确认

## 输入
窗户给我开30%

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"开启","对象":"窗户","区域":"null","数值":"30.0"}]}
```

首字节等待：560.584 ms；完整返回：857.832 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"开启","对象":"窗户","区域":"null","数值":"30.0"}]}
```

首字节等待：551.511 ms；完整返回：816.593 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"开启","对象":"窗户","区域":"null","数值":"30.0"}]}
```

首字节等待：519.009 ms；完整返回：782.239 ms；合法 JSON：True

初步判断：待人工确认

## 输入
把空调调到24度

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"调整","对象":"空调温度","区域":"车内","数值":"24"}]}
```

首字节等待：541.001 ms；完整返回：809.048 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"调整","对象":"空调温度","区域":"车内","数值":"24"}]}
```

首字节等待：459.409 ms；完整返回：729.624 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"调整","对象":"空调温度","区域":"车内","数值":"24"}]}
```

首字节等待：462.681 ms；完整返回：749.928 ms；合法 JSON：True

初步判断：待人工确认

## 输入
打开天窗

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"打开","对象":"天窗","区域":"车内","数值":null}]}
```

首字节等待：510.056 ms；完整返回：759.806 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"打开","对象":"天窗","区域":"车内","数值":"无"}]}
```

首字节等待：506.473 ms；完整返回：753.668 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"打开","对象":"天窗","区域":"车内","数值":"无"}]}
```

首字节等待：531.014 ms；完整返回：773.039 ms；合法 JSON：True

初步判断：待人工确认

## 输入
把天窗关上

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"关闭","对象":"天窗","区域":"车内","数值":"无"}]}
```

首字节等待：535.709 ms；完整返回：790.919 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"关闭","对象":"天窗","区域":"车内","数值":"无"}]}
```

首字节等待：501.005 ms；完整返回：777.414 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"关闭","对象":"天窗","区域":"车内","数值":"无"}]}
```

首字节等待：564.229 ms；完整返回：809.24 ms；合法 JSON：True

初步判断：待人工确认

## 输入
打开右前门，然后关闭左后车窗

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"关闭","对象":"车窗","区域":"左后","数值":null},{"动作":"关闭","对象":"车窗","区域":"左后","数值":null},{"动作":"打开","对象":"车门","区域":"右前","数值":null}]}
```

首字节等待：576.62 ms；完整返回：1256.727 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"关闭","对象":"车窗","区域":"左后","数值":null},{"动作":"关闭","对象":"车门","区域":"右前","数值":null}]}
```

首字节等待：534.323 ms；完整返回：991.695 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"关闭","对象":"车窗","区域":"左后","数值":null},{"动作":"关闭","对象":"车门","区域":"右前","数值":null}]}
```

首字节等待：517.329 ms；完整返回：971.937 ms；合法 JSON：True

初步判断：待人工确认

## 输入
打开右前门，关闭左前车窗，打开天窗

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"关闭","对象":"左前车窗","区域":"null","数值":"null"},{"动作":"打开","对象":"右前门","区域":"null","数值":"null"},{"动作":"打开","对象":"天窗","区域":"null","数值":"null"}]}
```

首字节等待：522.534 ms；完整返回：1203.726 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"关闭","对象":"左前车窗","区域":"null","数值":"null"},{"动作":"打开","对象":"右前门","区域":"null","数值":"null"},{"动作":"打开","对象":"天窗","区域":"null","数值":"null"}]}
```

首字节等待：570.23 ms；完整返回：1233.935 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"关闭","对象":"左前车窗","区域":"null","数值":"null"},{"动作":"打开","对象":"右前门","区域":"null","数值":"null"},{"动作":"打开","对象":"天窗","区域":"null","数值":"null"}]}
```

首字节等待：534.702 ms；完整返回：1201.075 ms；合法 JSON：True

初步判断：待人工确认

## 输入
先关窗，再打开空调

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"关闭","对象":"窗户","区域":"车内","数值":null},{"动作":"开启","对象":"空调","区域":"车内","数值":null}]}
```

首字节等待：511.49 ms；完整返回：924.581 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"关闭","对象":"窗户","区域":"车内","数值":null},{"动作":"开启","对象":"空调","区域":"车内","数值":null}]}
```

首字节等待：543.875 ms；完整返回：984.662 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"关闭","对象":"窗户","区域":"车内","数值":null},{"动作":"开启","对象":"空调","区域":"车内","数值":null}]}
```

首字节等待：510.677 ms；完整返回：959.402 ms；合法 JSON：True

初步判断：待人工确认

## 输入
把左后车窗开到一半，然后打开右前门

## 模型输出与耗时

### 第 1 次

```json
{"子意图":[{"动作":"开启","对象":"车窗","区域":"左后侧","数值":"一半"},{"动作":"开启","对象":"门","区域":"右前侧"}]}
```

首字节等待：551.437 ms；完整返回：1027.047 ms；合法 JSON：True

### 第 2 次

```json
{"子意图":[{"动作":"开启","对象":"车窗","区域":"左后侧","数值":"一半"},{"动作":"开启","对象":"门","区域":"右前侧"}]}
```

首字节等待：401.112 ms；完整返回：836.334 ms；合法 JSON：True

### 第 3 次

```json
{"子意图":[{"动作":"开启","对象":"车窗","区域":"左后侧","数值":"一半"},{"动作":"开启","对象":"门","区域":"右前侧"}]}
```

首字节等待：526.419 ms；完整返回：978.672 ms；合法 JSON：True

初步判断：待人工确认

## 总体统计

```json
{
  "total_calls": 42,
  "json_valid_calls": 42,
  "first_chunk_average_ms": 514.85,
  "first_chunk_median_ms": 519.266,
  "total_average_ms": 853.077,
  "total_median_ms": 799.562,
  "total_min_ms": 661.329,
  "total_max_ms": 1256.727
}
```
