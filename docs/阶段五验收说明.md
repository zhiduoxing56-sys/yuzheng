# 阶段五验收说明

## 范围

本阶段只实现报告中的可信输入层和语音指令处理：PC 麦克风、模拟车载阵列通道、频谱异常、LA 合成音检测、PA 重放检测、可信评分、Whisper ASR、区域权限及其到既有安全裁决链的连接。未开始前端、CARLA、真实 CAN、真实台架或后续实验复现。

## 环境与模型

- Python：`D:\software\anaconda\envs\yuzheng311\python.exe`
- 语义向量：`BAAI/bge-base-zh-v1.5`，768 维真实推理
- 索引：hnswlib 0.8.0，`degraded=false`
- LA：`MelodyMachine/Deepfake-audio-detection-V2@de3cde5`
- PA：`Vansh180/deepfake-audio-wav2vec2@e1197e2`
- ASR：`openai/whisper-base@e37978b`
- 麦克风：sounddevice 0.5.3，实际识别到 Realtek 录音设备

提交前 `scripts/preflight_real_runtime.py` 实际返回 `ok=true`、BGE `local_sentence_transformer`、维度 768、向量范数 0.99999997、`real_model_inference=true`、索引 `hnswlib`、`degraded=false`、SQLite connected。

## 自动测试

阶段五新增 `backend/tests/stage5/test_trusted_voice_pipeline.py`，参数化后收集 26 项；与冻结基线合计 113 项。测试不仅断言状态码，还断言真实波形指标、模型推理元数据、风险关系、转写文本、语义帧、证据子图、区域差异、声学复核后风险保持、裁决、令牌边界、WebSocket 顺序、审计写入、无原始音频、BGE/HNSW 非降级和双链校验。

最终命令已执行；编译命令无输出且退出码为 0，全量结果为 `113 passed, 1 warning in 240.03s (0:04:00)`：

```powershell
D:\software\anaconda\envs\yuzheng311\python.exe -m compileall -q backend\app backend\tests
D:\software\anaconda\envs\yuzheng311\python.exe -m pytest -v --durations=60
```

## 实际服务与样本结果

最终实际服务使用 `D:\software\anaconda\envs\yuzheng311\python.exe -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8016` 启动，进程号 52800。健康探测约 5219 ms 后返回 `status=ok`；BGE 为 `local_sentence_transformer`，HNSW 为 `hnswlib`、768 维、`degraded=false`。验收结束后进程已停止。

1. 实际生成的 Edge TTS“打开车门”：LA=0.000000、PA=0.857356、trust=0.542942、声学 REVIEW；Whisper 真实输出“打開車門。”，规范化后为“打开/车门”，最终 REVIEW，无授权令牌。
2. 实际扬声器播放后由麦克风重录的“打开车门”：LA=0.821156、PA=0.639686、replay_risk=0.360314、trust=0.784337、声学 REVIEW；Whisper 实际误识别为“我们虽然学得科目多”，最终 REVIEW，无授权令牌。该结果显示本次真实重放样本的 PA 风险高于其 bonafide 分数，但也暴露单样本泛化与环境敏感性。
3. 公共真人中文样本：LA=0.546577、PA=0.931629、trust=0.791282、声学 PASS；Whisper 返回真实中文句子。该文件只作为公开真人基准，不冒充团队成员录音。
4. 实际 PC 麦克风服务请求：采集到非静音信号，RMS=0.143704；LA=0.000018、PA=0.161712、trust=0.264692，安全 BLOCK，ASR 未执行，无令牌。该输入没有形成可验证的人类车控口令，因此只证明真实设备采集和安全终止路径。

最终服务中首条公开真人样本请求约 3079.72 ms（同时触发 LA/PA/ASR 首次加载），随后合成音约 818.32 ms、重放音约 977.88 ms。此前实际 PC 麦克风请求（含 3 秒录音）约 3811 ms。以上是对应运行中的真实单次结果，不冒充完整冷启动模型耗时。最终三个服务请求的审计链为 `valid=true`，三条工作流链均为 `true`；HNSW 规范节点 33。

## 已知限制

- 验收现场没有取得可用的团队成员真人“打开车门”和“查询当前速度”录音；公开真人样本不能替代该物理验收要求。真实 PC 麦克风入口已运行，但采集内容未产生可靠车控转写。
- LA/PA 公开模型的训练语料、语言和录制环境与当前中文座舱样本并不完全匹配；本阶段只陈述已测试样本结果，不宣称覆盖全部合成或重放攻击。
- Whisper base 在扬声器重录样本上发生明显误识别；声学 REVIEW 和既有语义歧义/安全门会阻止其直接执行。
- 模拟阵列通道是确定性座位标签输入，不是物理阵列波束形成或声源定位。
