# 低风险舒适指令裁决修复结果（2026-08-04）

## 原始真实轮次追踪

截图对应的最新记录为 `TURN_744e16b66cde / AUD_b32749c3d539`。仅提取安全裁决字段，未读取或输出授权令牌、原始音频和大段审计载荷。

- 语义：动作“打开”、目标“空调”，R1，语义可信度 1.0、歧义 0。
- 必查证据：空；证据需求为空；缺失证据为空。
- 证据覆盖适用性：false；ECR=null；ECS=1.0；EF=0.95；SAS=0.418023；EAS=0.805546；冲突数 0。
- 原证据对齐路由：`EVIDENCE_REVIEW`。
- 评分：安全评分 0.949，评分结论 PASS。
- 安全门：未阻断，无命中规则。
- 裁决来源：SAFETY_GATE、EVIDENCE_ALIGNMENT、SAFETY_SCORE。
- 原最终裁决：REVIEW。
- 原合并原因：证据对齐要求 REVIEW，保守等级合并把 score_decision=PASS 提升为 REVIEW。
- 具体错误分支：`EvidenceQualityService.alignment_route()` 将 0.60 ≤ EAS < 0.85 映射为 `EVIDENCE_REVIEW`；该分支在 `required_types=[]` 时仍被调用。

## 根因修复

- 没有必查证据时仍保留 EAS 数值用于技术审计，但 `evidence_alignment_route` 固定为 `EVIDENCE_PASS`；现有 `evidence_coverage_applicable=false` 表示业务上不适用。
- 有必查证据时继续使用原 EAS 阈值和保守合并。
- 场景快照和显式传感器覆盖现在即使不进入语义 TopK，也参与冲突检测；因此低风险舒适指令不会因“证据对齐不适用”而忽略真实传感器冲突。
- 普通空调开关、温度和风量归为 R1 舒适功能，无必查证据。
- 前挡风除雾使用独立语义目标；关闭操作要求天气和车速证据。浓雾关闭前挡风除雾命中独立硬门，不与普通空调规则混用。

## 前端解释

- 裁决页、复核结果和审计详情并列显示安全评分/评分判断、证据对齐判断和最终裁决。
- 明确显示将评分判断提升到最终裁决的模块；没有提升时明确说明沿用评分判断。
- 默认使用结构化中文，内部合并字符串和完整原因移入“技术详情”。
- 车辆状态按当前目标筛选；普通空调没有后端状态字段时只显示“空调状态未接入”。
- 可写复核空闲态显示“等待复核”，只读轮次显示“只读”；执行空闲和 `NOT_EXECUTED` 显示“尚未执行”。

## 回归与真实验收

- 明确驾驶员“帮我打开空调”：PASS。
- 车辆行驶中“帮我打开空调”：PASS。
- “把那个打开”：REVIEW。
- 低可信语音“帮我打开空调”：由 VOICE_TRUST 提升为 REVIEW，无令牌。
- 浓雾关闭前挡风除雾：命中 `DENSE_FOG_FRONT_DEFOG_OFF_PROHIBITED`，BLOCK。
- 多来源车速冲突下打开空调：不会直接 PASS。
- 后端 342 项用例分组闭环；前端 60 项测试、类型检查和生产构建通过。

真实 HTTP 新轮次 `TURN_adf3f7c4e906`：R1、必查证据为空、EAS=0.809628、证据路由 PASS、安全评分 0.949、评分 PASS、安全门通过、最终 PASS。

真实浏览器新轮次 `TURN_bbf1102548c4`：页面展示“安全评分 0.949 / 评分判断允许执行 / 证据对齐不适用 / 最终裁决允许执行”，同时展示“空调状态未接入”和“尚未执行”；浏览器控制台无错误。
