# 审计性能专项基线与验收（2026-08-04）

本文只记录数量、字节数、状态码和耗时，不包含授权令牌、指令正文、审计 JSON 或其他敏感载荷。

## 数据规模

- 审计链总记录数：111
- 命令审计数：110
- `audit_records.record_json` 总大小：47,175,356 字节
- 单条最大 `record_json`：1,747,450 字节
- 数据库文件：50,401,280 字节

## 修改前在线接口基线

旧 8765 进程运行时间较长，在线列表当时返回 42 条可见命令审计；耗时用于记录旧接口全量解析和并发叠加特征。同一 110 条正式库的算法级逐字段对比另见下节。

| 场景 | 耗时 |
|---|---:|
| `/api/audits?page_size=1` | 5.738 s |
| `/api/audits?page_size=20` | 6.434 s |
| `/api/audits?page_size=100` | 6.034 s |
| `decision=PASS` | 5.738 s |
| 时间范围筛选 | 5.893 s |
| `/api/audits/verify-chain` | 8.202 s |
| 两个列表并发 | 12.059 s / 12.251 s，墙钟 12.326 s |
| 两个链校验并发 | 17.016 s / 16.997 s，墙钟 17.017 s |

页大小几乎不影响旧列表耗时，证明分页前已完成整库解析；两个相同请求并发时耗时近似翻倍，证明没有共享计算。

## 同一 110 条库的算法一致性与耗时

旧算法和新算法在内存中逐字段比较 `audit_id`、`turn_id`、时间、指令摘要、初始/原始/有效裁决、执行状态和语义帧，以下场景全部一致：默认第一页、第二页、100 条页、PASS、REVIEW、BLOCK 和时间范围。

| 场景 | 旧算法 | 新算法 |
|---|---:|---:|
| 默认 20 条 | 2.343 s | 0.213 s |
| 第 2 页 20 条 | 2.283 s | 0.105 s |
| 100 条 | 2.459 s | 0.461 s |
| PASS | 2.344 s | 0.199 s |
| REVIEW | 2.420 s | 0.102 s |
| BLOCK | 2.292 s | 0.105 s |
| 时间范围 | 2.272 s | 0.220 s |

## 修改后真实接口验收

正式迁移后接口返回 110 条命令审计。

| 场景 | 耗时 |
|---|---:|
| `/api/audits?page_size=1` | 0.0450 s |
| `/api/audits?page_size=20` | 0.3457 s |
| `/api/audits?page_size=100` | 0.8938 s |
| `decision=PASS` | 0.2375 s |
| 时间范围筛选 | 0.5858 s |
| 两个列表并发 | 0.7748 s / 0.8952 s，墙钟 0.9039 s |
| 已缓存链校验 | 0.0278 s |
| 两个已缓存链校验并发 | 0.0301 s / 0.0296 s，墙钟 0.0309 s |

仓储层首次完整校验为 0.9088 秒，随后缓存命中为 0.0009 秒。完整校验读取快照后先释放 SQLite 事务，再执行 JSON 散列；专项并发测试验证散列期间健康查询仍可响应。

正式服务并发验收中，独立首次完整校验耗时 1.2379 秒；同一时间健康接口 0.0212 秒返回 200，文本指令接口 3.8966 秒返回 200 且正常受理。该指令追加新审计后，服务端增量链校验耗时 0.0521 秒并返回有效。

浏览器真实页面验收确认：初次进入审计页只加载 111 条列表，不自动请求全局链；链区域显示“开始校验”，点击后返回“全局审计链通过”；非裁决页分别显示“后端健康”和“当前页面未启用实时通道”。连续选择 PASS 后立即切换 BLOCK，最终地址和 20 条可见记录均保持 BLOCK，迟到响应未覆盖最新筛选。

## 迁移结果

- 迁移前命令审计：110
- 迁移后命令审计：110
- 摘要记录：110
- `audit_id`、`turn_id`、`decision`、`created_at` 不一致：0
- 原始审计载荷改写：0
- 正式迁移备份：`data/database/backups/yuzheng-pre-audit-summary-20260804T075319Z.db`

迁移完成后的并发验收额外提交了一条无敏感内容的“查询当前速度”指令，因此页面最终命令审计数为 111；该新增发生在迁移一致性检查之后，不影响上述迁移前后 110=110 的结论。

## 自动化检查

- 后端全量：331 passed，1 个既有 TestClient 弃用警告。
- 前端：46 passed。
- TypeScript 检查：通过。
- Vite 生产构建：通过；保留既有三维图谱懒加载块体积提示，本阶段未进入图谱或视觉重构。
