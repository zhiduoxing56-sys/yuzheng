# 四十亿参数短输出高约束提示词实验

4B 仅在隔离 POC 中将完整原文转换为有序短规范句，输出中文四类 `车控/上下文/助手/不确定`。第一轮保持完整 JSON Schema、`think=false`、温度 0、128 token 上限；24 个样本各两次。按语义等价而非字面一致评分，单列否定、上下文、多意图和安全错误。只有最长合法输出低于 80 token 才进行第二轮“最大值加 25%”上限实验；任何安全关键错误即停止，不进入生产链。
