# 面向智能座舱车控的可验证硬规则安全门与因果贝叶斯协同裁决重构方案

## 对当前上传版本的核心判断

我已经把当前上传的 `example (6).tex` 中第二章的主线重新顺了一遍，重点看了第二节“安全知识库驱动的语义车控对齐方案”、现有因果贝叶斯算法，以及准备整体删除的第三节“贯穿车控链路的统一安防框架”。我认为你的理解完全正确，而且这次修改方向会让整篇报告的技术主线明显更强。

当前版本最大的结构性问题并不在安全门公式本身，而在于**安全门、因果贝叶斯和五维评分三者的技术边界没有真正拉开**。尤其现有第三节把

\[
V_{\mathrm{causal}}
=
\mathbb{I}
[p_{\mathrm{safe}}<\theta_{\mathrm{safe}}]
\]

直接并进

\[
B_{\mathrm{gate}}
=
V_{\mathrm{rule}}
\lor
V_{\mathrm{evidence}}
\lor
V_{\mathrm{conflict}}
\lor
V_{\mathrm{causal}},
\]

这样写以后，在论文逻辑上，因果贝叶斯实际上被安全门“吞进去”了。评审看到这里很难回答三个关键问题：**安全门自身究竟解决了什么问题？因果贝叶斯自身解决了什么问题？二者为什么同时存在时明显优于任一单模块？**

这三个问题恰恰应该成为新第三节最主要的技术论证。

我的建议是：**现有第三节整体删除后，不再做一个范围很大的“统一安防框架”，直接把第三节收缩为一个非常聚焦的关键技术：可验证硬规则安全门。与此同时，把安全门和第二节已经建立的因果贝叶斯明确设计成两条相互独立、覆盖不同风险空间的安全防线，再让原来的五维评分承担剩余不确定性的柔性裁决。**

新的整体关系建议明确成：

\[
\boxed{
\text{安全知识与证据}
\rightarrow
\underbrace{\text{硬规则安全门}}_{\text{确定性红线}}
\rightarrow
\underbrace{\text{因果贝叶斯推理}}_{\text{组合与概率风险}}
\rightarrow
\underbrace{\text{五维安全评分}}_{\text{剩余不确定性}}
\rightarrow
\text{执行决策}
}
\]

三层之间采用**非对称裁决关系**：

\[
\boxed{
\text{硬规则可以否决}
\quad
\text{因果风险可以否决或收紧}
\quad
\text{五维评分只能处理前两层放行后的剩余空间}
}
\]

这样一来，安全门的定位会非常清楚：

> **安全门负责“确定不能做”的事情；因果贝叶斯负责“没有显式禁止，但组合起来可能危险”的事情；五维评分负责“没有越过硬边界且因果风险可接受，但证据、语义和上下文仍存在不确定性”的事情。**

这与当前汽车安全体系的发展方向也高度一致。现行国际标准本身就分别关注故障导致的功能安全、预期功能不足造成的安全风险、网络安全风险和人工智能安全风险，而没有假定单一安全机制能够覆盖所有危险来源。国际标准化组织的道路车辆功能安全标准用于处理安全相关电子电气系统故障导致的危险；预期功能安全标准进一步处理规格不足或功能性能不足引发的不合理风险；道路车辆网络安全工程标准覆盖车辆全生命周期网络安全风险；二〇二四年的道路车辆人工智能安全规范又在既有功能安全和预期功能安全方法基础上扩展了人工智能安全框架。citeturn0search1turn0search2turn0search3turn6search0turn6search1

因此，新第三节完全可以围绕一个非常鲜明的科学问题展开：

> **如何从法规、安全标准和工程安全知识中自动构建具有来源依据、形式语义、版本约束和运行时可执行能力的硬安全规则，并通过一个独立于概率模型和生成式模型的确定性安全门，为智能座舱车控建立不可被评分补偿的安全底线；进一步通过因果贝叶斯弥补有限硬规则对复杂连续风险覆盖不足的问题。**

这比当前“输入安全、模型安全、知识安全、授权安全、审计安全全部铺开”的统一安防框架更聚焦，也更容易形成一个评审一眼就能够记住的关键技术。

## 硬规则安全门应该真正做成什么

### 先严格限制安全门的能力边界

这里有一个非常重要的设计原则：**千万不要把所有安全知识全部转换成硬规则。**

一旦这么做，你们后面很难解释为什么还需要因果贝叶斯。

建议在第二节已经构建出来的可信安全知识集合

\[
\mathcal K_{\mathrm{trusted}}
\]

之上，再增加一个非常关键的**“硬规则晋升机制”**。只有满足严格条件的知识节点，才允许从一般安全知识晋升为

\[
\mathcal K_{\mathrm{hard}}.
\]

我建议定义一个硬规则资格判定函数：

\[
\operatorname{HardEligible}(K_i)
=
A_i
\land
D_i
\land
O_i
\land
F_i
\land
N_i,
\]

其中：

\[
A_i=\text{来源权威性},
\]

\[
D_i=\text{规则结论确定性},
\]

\[
O_i=\text{运行条件可观测性},
\]

\[
F_i=\text{规则形式可执行性},
\]

\[
N_i=\text{风险不可补偿性}.
\]

也就是说，一个知识节点要成为真正意义上的硬规则，需要同时满足五个条件。

**第一，来源必须足够权威。** 道路交通法律法规、现行强制性国家标准、适用的车辆准入要求，以及经过功能安全分析确认的车辆级安全约束可以进入这一层。动态事故情报、新闻、投诉、一般经验规则继续保留在安全知识库中，不能自动获得硬阻断权限。中国当前已经形成了相当完整的车辆安全和网络安全规范基础。例如，现行《机动车运行安全技术条件》GB 7258—2017仍处于现行状态，同时已经进入修订流程；GB 44495—2024《汽车整车信息安全技术要求》已经于二〇二六年一月一日实施；GB 44496—2024《汽车软件升级通用技术要求》也已于同日实施；道路车辆功能安全国家标准系列GB/T 34590—2022自二〇二三年七月实施。citeturn7search0turn7search1turn1search0turn1search1turn7search2turn2search2

**第二，规则必须具有确定结论。** 例如《中华人民共和国道路交通安全法实施条例》第五十八条明确规定，机动车在夜间没有路灯、照明不良或者低能见度情况下行驶时，应开启前照灯、示廓灯和后位灯；雾天还涉及雾灯和危险报警闪光灯。这种要求能够转化为明确的“必须保持某个执行后状态”的约束，非常适合作为硬安全规则。citeturn3search0turn3search1

例如：

\[
\text{车辆行驶}
\land
\text{低照度}
\Rightarrow
\text{前照灯执行后状态}=\mathrm{ON}.
\]

如果请求动作是关闭前照灯：

\[
a=\mathrm{HeadLampOff},
\]

安全门预测动作执行后的状态为：

\[
\hat s^{+}_{\mathrm{lamp}}=\mathrm{OFF},
\]

于是直接得到

\[
\mathrm{OFF}\neq\mathrm{ON},
\]

因此阻断。

**第三，触发规则所需状态必须能够可靠获得。** 如果某条规则涉及一个当前车辆系统根本无法测量的条件，它不适合成为在线硬门控条件。它仍可以参与因果贝叶斯和人工审核。

**第四，规则必须能够编译成确定性逻辑。** “不得打开”“必须保持关闭”“速度大于某阈值时不得执行”等条件非常适合；“可能产生较高风险”“应根据周边交通环境谨慎执行”等语句往往还需要概率推理。

**第五，风险必须具有不可补偿性。** 一旦命中，语义置信度再高、其它传感器再可靠、用户语气再紧急，都不能获得补偿。

这样会形成一个非常漂亮的集合关系：

\[
\boxed{
\mathcal K_{\mathrm{hard}}
\subset
\mathcal K_{\mathrm{trusted}}
}
\]

而不是

\[
\mathcal K_{\mathrm{hard}}
=
\mathcal K_{\mathrm{trusted}}.
\]

这一个关系非常重要。它在理论上直接给出了“为什么安全门不能解决所有问题”。

### 把硬规则构建本身做成关键技术

你前面提到“从这个硬规则怎么构建出来，用到哪些法律法规文件，如何基于数据库技术或者提取技术具体设计出来”，我认为这里完全可以做成第三节最有技术含量的一部分。

建议形成下面这一条完整流水线：

\[
\boxed{
\text{权威安全材料}
\rightarrow
\text{条款解析}
\rightarrow
\text{原子规范命题}
\rightarrow
\text{受约束语义抽取}
\rightarrow
\text{硬规则资格筛选}
\rightarrow
\text{形式化规则}
\rightarrow
\text{冲突验证}
\rightarrow
\text{人工审核}
\rightarrow
\text{签名发布}
}
\]

其中最关键的地方在于：**大模型只参与离线抽取，不拥有规则发布权限。**

这一思想与目前你的第二节其实非常兼容。第二节已经建立了“原始材料—原子安全命题—候选知识—可信知识”的链路。现在不需要推倒重来，只需要在可信知识之后增加：

\[
\mathcal K_{\mathrm{trusted}}
\xrightarrow{\mathrm{HardEligible}}
\mathcal K_{\mathrm{hard}}.
\]

建议每条硬规则统一表示为：

\[
R_i=
\left\langle
id_i,
src_i,
scope_i,
pre_i,
act_i,
post_i,
E_i^{H},
ttl_i,
exc_i,
pri_i,
ver_i,
hash_i
\right\rangle.
\]

其中：

| 字段 | 含义 |
|---|---|
| \(src_i\) | 来源法律、法规、标准、条款位置 |
| \(scope_i\) | 地区、车型、功能版本和车辆配置适用范围 |
| \(pre_i\) | 规则触发前提 |
| \(act_i\) | 受约束动作 |
| \(post_i\) | 动作执行后的必须满足状态 |
| \(E_i^{H}\) | 判断该规则必须取得的硬证据集合 |
| \(ttl_i\) | 每类动态证据最长有效时间 |
| \(exc_i\) | 法定或工程例外条件 |
| \(pri_i\) | 规则优先级 |
| \(ver_i\) | 规则版本和生效时间 |
| \(hash_i\) | 发布快照完整性摘要 |

这实际上已经不是简单的“规则表”，而是一个**可追溯、可验证、可执行、可版本化的安全规则数据库**。

规则表达技术可以借鉴现有成熟规范。法律规则标记规范能够表达义务、禁止、许可、来源以及规范规则之间的关系，适合解决“原始法律条文怎样对应机器规则”的问题；万维网联盟的形状约束语言可以对图结构数据执行约束验证；来源追踪本体可以保存实体、活动和来源之间的可追溯关系；决策模型与表示规范提供了精确表达决策表和业务规则的方法。citeturn4search4turn4search1turn4search2turn4search7

你们不需要把这些规范全部完整实现，可以吸收它们的思想。最实用的工程方案可以采用四层存储：

\[
\boxed{
\text{原文层}
+
\text{来源与版本层}
+
\text{规则关系层}
+
\text{在线执行层}
}
\]

原文层保存法规、标准及原始条款；关系层保存“规则—来源—条件—证据—动作—风险”的图关系；关系型数据库保存规则版本、生效时间、审核状态、地区和车型等强结构字段；在线安全门使用预编译规则快照，不在驾驶过程中临时执行复杂知识图谱查询。

尤其建议增加一个**离线规则编译器**：

\[
\text{结构化硬规则}
\xrightarrow{\text{编译}}
\text{确定性判定图}.
\]

在线系统实际运行判定图，规则知识图谱负责治理和解释。

这样能同时拥有：

**知识图谱的可追溯能力 + 数据库的版本治理能力 + 编译规则的低时延能力。**

正式发布前，再利用可满足性模理论求解器检查规则之间是否存在逻辑矛盾。微软研究院的Z3属于成熟的符号逻辑求解工具，可用于逻辑公式可满足性检查和软件验证。citeturn5search3turn5search13

例如两条规则：

\[
C\Rightarrow L=\mathrm{ON}
\]

和

\[
C\Rightarrow L=\mathrm{OFF}
\]

在相同适用范围同时生效时，离线发布流程应直接检测出冲突，禁止生成在线规则快照。

这会让你们的安全门从“几条人工写出的如果—那么规则”提升成：

> **法规与安全知识驱动、机器辅助抽取、形式验证、版本治理、确定性执行的硬规则安全门。**

这个技术层级就高很多了。

## 新安全门怎样实现真正的硬兜底

### 建议把安全门从“请求状态检查”升级到“执行后安全不变量检查”

你们当前“关闭前照灯”的例子方向完全正确，我建议进一步统一成**执行后安全状态约束**。

设当前可信车辆状态为

\[
\mathbf{s}(t),
\]

规范动作是

\[
a,
\]

对简单车控动作建立经过审核的确定性动作影响函数：

\[
\widehat{\mathbf{s}}^{+}
=
T_{\mathrm{act}}
\left(
\mathbf{s}(t),a
\right).
\]

例如：

\[
T_{\mathrm{act}}
(
\mathrm{HeadLamp}=\mathrm{ON},
\mathrm{HeadLampOff}
)
=
\mathrm{HeadLamp}=\mathrm{OFF}.
\]

对于硬规则 \(R_i\)，其安全不变量记为

\[
\phi_i
\left(
\widehat{\mathbf{s}}^{+}
\right).
\]

于是：

\[
V_i(t)
=
\mathrm{Active}_i(t)
\land
\neg
\phi_i
\left(
\widehat{\mathbf{s}}^{+}
\right).
\]

总体规则违反标志：

\[
V_{\mathrm{rule}}
=
\bigvee_{R_i\in\mathcal K_{\mathrm{hard}}^{\mathrm{active}}(t)}
V_i(t).
\]

这比简单判断“当前动作名称是否在黑名单”先进很多，因为同一个动作在不同状态下可能产生完全不同的结果。

例如：

\[
\text{打开车门}
\]

在

\[
v=0
\]

时可能正常，在

\[
v>v_{\mathrm{door}}
\]

时可能触发硬规则。

因此硬规则表达的是：

\[
\boxed{
\text{当前场景}
+
\text{请求动作}
\rightarrow
\text{执行后状态是否违反安全不变量}
}
\]

这会和第二节的“面向执行后安全状态的因果贝叶斯推理”形成非常漂亮的呼应：**两条链路都研究执行后状态，一条给出确定性约束，一条给出概率安全性。**

### 强制证据也需要进一步做“最小安全证明集”

我建议不要继续采用

\[
\forall e\in\mathcal E_{\mathrm{req}}
\]

只要任意证据缺失就全部硬阻断的设计。

这样很容易被评审质疑过于保守。

应该给每条硬规则单独定义**最小硬证据集**

\[
\mathcal E^{H}_i.
\]

例如关闭前照灯的某条规则真正判定所需的证据可能是：

\[
\mathcal E_i^{H}
=
\{
\text{车辆运动状态},
\text{照明环境状态},
\text{当前灯光状态}
\}.
\]

一个其它无关传感器失效不应该阻断该规则判断。

于是安全门的强制证据条件变成：

\[
V_{\mathrm{evi}}
=
\bigvee_{R_i\in
\mathcal K_{\mathrm{hard}}^{\mathrm{relevant}}
}
\left[
\exists e\in\mathcal E_i^{H},
\;
\tau(e)\in
\{
\mathrm{MISSING},
\mathrm{TAMPERED}
\}
\right].
\]

对于动态高风险动作，再加入：

\[
V_{\mathrm{fresh}}
=
\bigvee_i
\left[
a\in\mathcal A_{\mathrm{critical}}
\land
\exists e\in\mathcal E_i^H,
\;
\Delta t_e >
TTL_e
\right].
\]

这样形成一种非常重要的思想：

\[
\boxed{
\text{无法证明满足硬安全边界}
\Rightarrow
\text{高风险动作不执行}
}
\]

同时：

\[
\boxed{
\text{与当前硬边界无关的证据异常}
\not\Rightarrow
\text{无条件硬阻断}
}
\]

这一改动能够显著改善正常指令误拒问题。

### 推荐的新安全门公式

我建议最终把安全门本身设计得非常纯粹，**彻底删除原来的 \(V_{\mathrm{causal}}\)**：

\[
\boxed{
B_{\mathrm{hard}}
=
V_{\mathrm{rule}}
\lor
V_{\mathrm{mandatory}}
\lor
V_{\mathrm{integrity}}
\lor
V_{\mathrm{authority}}
}
\]

其中：

\[
V_{\mathrm{rule}}
=
\mathbb I
\left[
\exists R_i:
\mathrm{Active}_i(t)
\land
\neg\phi_i(\widehat{\mathbf s}^{+})
\right],
\]

表示执行后状态违反当前生效硬规则；

\[
V_{\mathrm{mandatory}}
=
\mathbb I
\left[
\exists
e\in
\mathcal E_i^{H},
\;
e
\text{缺失或关键动态证据严重过期}
\right],
\]

表示无法完成硬安全条件核验；

\[
V_{\mathrm{integrity}}
=
\mathbb I
\left[
\exists
e\in
\mathcal E_i^{H},
\;
\tau(e)=\mathrm{TAMPERED}
\right],
\]

表示关键安全证据完整性失效；

\[
V_{\mathrm{authority}}
=
\mathbb I
\left[
\text{请求要求绕过安全规则、伪造系统模式或伪造控制权限}
\right].
\]

于是：

\[
B_{\mathrm{hard}}=1
\Longrightarrow
D=\mathrm{BLOCK}.
\]

并规定：

\[
\boxed{
B_{\mathrm{hard}}=1
\Rightarrow
\text{后续因果概率与五维评分均无权恢复执行}
}
\]

这一条需要在文中写成系统级安全不变量。

GB 44495—2024已经作为我国现行强制性汽车整车信息安全标准，于二〇二六年一月一日实施，并覆盖汽车信息安全管理体系、外部连接、通信、软件升级、数据等相关技术要求；联合国第155号法规则围绕车辆网络安全和网络安全管理体系提出车辆全生命周期要求。这些规范可以为安全资产可信发布、规则完整性、执行接口保护和安全更新治理提供上位工程依据。citeturn1search0turn1search11turn0search0turn0search6

这里还可以引入一个很先进的设计：**安全门运行在独立确定性安全执行域中。**

即：

\[
\text{大模型}
\rightarrow
\text{结构化请求}
\]

以后，大模型再也没有任何权限改变：

\[
\mathcal K_{\mathrm{hard}},
\quad
\mathcal E_i^H,
\quad
T_{\mathrm{act}},
\quad
\theta_{\mathrm{hard}}.
\]

正式规则快照经过审核和完整性校验之后加载。类似的“外部运行时安全约束”思想广泛用于安全关键自主系统研究，运行时安全保障通过独立安全监视和约束机制限制复杂自主控制器的可执行行为。citeturn5search4turn5search0

这样评审很容易理解你所谓的“硬兜底”究竟硬在哪里：

\[
\boxed{
\text{它不依赖大模型判断}
}
\]

\[
\boxed{
\text{它不依赖概率阈值给予安全红线通融}
}
\]

\[
\boxed{
\text{它不允许五维高分补偿硬规则违反}
}
\]

\[
\boxed{
\text{它运行在动作真正进入执行接口之前}
}
\]

## 安全门为什么必须和因果贝叶斯同时存在

这部分我认为应该成为新第三节最精彩的理论论证。

### 安全门天然存在规则覆盖边界

安全门最强的场景是：

\[
\text{明确条件}
\Rightarrow
\text{明确禁止或必须状态}.
\]

例如：

\[
v>v_0
\land
a=\text{打开车门}
\Rightarrow
\text{阻断},
\]

或者：

\[
\text{低能见度}
\land
\text{车辆行驶}
\land
a=\text{关闭前照灯}
\Rightarrow
\text{阻断}.
\]

但是很多车辆风险并没有一个单独条件能够直接构成违法或硬安全冲突。

例如变道：

\[
v=70\mathrm{km/h},
\]

可能合法；

目标车道存在车辆，也可能合法；

侧向空间较小，也可能暂时还没有达到某一个固定禁止阈值；

道路湿滑单独出现也未必禁止变道。

然而：

\[
\text{较高车速}
+
\text{目标车接近}
+
\text{侧向间距较小}
+
\text{低附着}
\]

组合以后，执行变道的安全概率可能急剧下降。

如果强行把这种场景写成规则：

\[
v>63.4
\land
d<11.7
\land
\mu<0.54
\land
\Delta v>4.2
\Rightarrow
\mathrm{BLOCK},
\]

你很快会面对海量阈值组合。

这就是硬规则的天然边界：

\[
\boxed{
\text{规则擅长确定性不变量}
}
\]

\[
\boxed{
\text{规则不擅长高维连续变量之间的风险耦合}
}
\]

第二节的因果贝叶斯正好处理这个问题。

你当前已经定义：

\[
p_{\mathrm{safe}}(t)
=
P
\left(
Y_{\mathrm{post}}=\mathrm{SAFE}
\mid
do(A=\mathrm{EXECUTE}),
\mathbf E_{\mathrm{obs}}(t)
\right),
\]

它回答的是：

> 在当前车辆状态和证据条件下，如果真的执行这个动作，车辆随后处于安全状态的概率有多大？

因此它能够覆盖大量没有直接硬规则命中的组合风险。

### 因果贝叶斯同样不能取代安全门

反过来，如果只依赖因果贝叶斯也存在明显问题。

例如某一法规要求：

\[
C\Rightarrow L=\mathrm{ON},
\]

当前用户请求：

\[
L\rightarrow\mathrm{OFF}.
\]

即使因果网络由于参数误差给出：

\[
p_{\mathrm{safe}}=0.91,
\]

系统仍然不应该执行。

因为：

\[
\boxed{
\text{法规和安全不变量不应被一个概率模型重新投票}
}
\]

还有更重要的一点：因果贝叶斯需要依赖网络结构、条件概率参数以及运行时观测。当前第二节本身也规定了网络结构由车辆机理和审核安全知识离线确定、参数由历史和仿真数据估计。这意味着它仍然存在模型覆盖范围和参数校准范围。国际标准对功能安全、预期功能不足和人工智能安全分别设置不同的安全活动，本身也反映了复杂安全系统需要处理不同来源的不确定性与危险机制。citeturn0search1turn0search2turn6search0

还有一个很典型的攻击场景：

攻击者篡改车速证据：

\[
v_{\mathrm{fake}}=0.
\]

如果直接把这个观测输入因果贝叶斯：

\[
P(Y_{\mathrm{post}}=\mathrm{SAFE}\mid v=0,\ldots)
\]

很可能得到高安全概率。

所以：

\[
\boxed{
\text{证据完整性检查必须位于概率推理之前}
}
\]

只要关键证据被确认篡改：

\[
V_{\mathrm{integrity}}=1,
\]

安全门直接阻断：

\[
B_{\mathrm{hard}}=1.
\]

这样可以把二者的互补关系总结成一个非常清晰的表：

| 风险类型 | 硬规则安全门 | 因果贝叶斯 |
|---|---:|---:|
| 明确违反法规或安全不变量 | 极强 | 不应承担最终否决依据 |
| 强制证据缺失或篡改 | 极强 | 输入可靠性受影响 |
| 权限或系统模式伪造 | 极强 | 作用有限 |
| 多变量连续风险耦合 | 有限 | 强 |
| 没有现成规则的复杂边界场景 | 有限 | 强 |
| 执行动作后的概率风险 | 只能覆盖明确约束 | 强 |
| 新出现但尚未形成正式硬规则的风险组合 | 弱 | 可提供一定推理能力 |
| 法定禁止事项 | 强制确定性阻断 | 不允许覆盖硬规则 |

这张表很适合直接放进论文。

它能够明确说明：

\[
\boxed{
\mathcal R_{\mathrm{hard}}
\neq
\mathcal R_{\mathrm{causal}}
}
\]

并且：

\[
\boxed{
\mathcal R_{\mathrm{hard}}
\cup
\mathcal R_{\mathrm{causal}}
>
\mathcal R_{\mathrm{hard}}
}
\]

以及：

\[
\boxed{
\mathcal R_{\mathrm{hard}}
\cup
\mathcal R_{\mathrm{causal}}
>
\mathcal R_{\mathrm{causal}}
}
\]

这里的“大于”表示安全风险覆盖范围更完整。

### 最推荐的协同方式：非对称串联，而不做简单加权

千万不要把

\[
p_{\mathrm{safe}}
\]

直接作为第六个评分因子：

\[
S=
\alpha_1C_1+\cdots+\alpha_5C_5+\alpha_6p_{\mathrm{safe}}.
\]

这样会出现危险补偿：

\[
C_{\mathrm{sem}}\uparrow
\]

可能抵消

\[
p_{\mathrm{safe}}\downarrow.
\]

安全体系不应该允许这种补偿。

我推荐：

\[
\boxed{
\text{硬规则门}
\rightarrow
\text{因果安全包络}
\rightarrow
\text{五维评分}
}
\]

设安全门：

\[
B_{\mathrm{hard}}\in\{0,1\}.
\]

安全门通过后，再计算：

\[
p_{\mathrm{safe}}.
\]

定义两个因果安全阈值：

\[
\theta_{\mathrm{causal}}^{B}
<
\theta_{\mathrm{causal}}^{P}.
\]

则：

\[
D=
\begin{cases}
\mathrm{BLOCK},
&
B_{\mathrm{hard}}=1,
\\[4pt]
\mathrm{BLOCK},
&
B_{\mathrm{hard}}=0
\land
p_{\mathrm{safe}}
<
\theta_{\mathrm{causal}}^{B},
\\[4pt]
\mathrm{REVIEW},
&
\theta_{\mathrm{causal}}^{B}
\le
p_{\mathrm{safe}}
<
\theta_{\mathrm{causal}}^{P},
\\[4pt]
D_{5},
&
p_{\mathrm{safe}}
\ge
\theta_{\mathrm{causal}}^{P}.
\end{cases}
\]

其中

\[
D_5
\]

才是原来的五维评分结果。

这样结构特别清楚：

\[
\boxed{
\text{硬规则低层红线}
+
\text{因果中层风险包络}
+
\text{五维上层柔性裁决}
}
\]

我甚至建议再增加一个很有创新性的**“单向收紧机制”**。

当因果概率虽然没有低到直接阻断，但接近风险区时，不让它给五维评分加分，仅提高通过阈值：

\[
\theta_{\mathrm{pass}}^{\mathrm{dyn}}
=
\min
\left[
1,\;
\theta_{\mathrm{pass}}^{0}
+
\gamma
\left(
\theta_{\mathrm{causal}}^{P}
-
p_{\mathrm{safe}}
\right)_{+}
\right].
\]

这里

\[
(x)_+=\max(x,0).
\]

所以因果贝叶斯只可能：

\[
\text{保持原阈值}
\]

或者

\[
\text{让通过条件更严格}.
\]

永远不会出现：

\[
p_{\mathrm{safe}}\text{较高}
\Rightarrow
\text{降低安全门要求}.
\]

这就是我非常推荐的**非对称安全耦合**。

它会让你们的设计明显区别于普通“多个指标加权求和”。

## 五维评分怎么保留，同时避免和安全门抢功能

原来的五维评分其实可以保留，而且保留之后三层结构更加完整：

\[
C_{\mathrm{sem}},
\quad
C_{\mathrm{cov}},
\quad
C_{\mathrm{trust}},
\quad
C_{\mathrm{jb}},
\quad
C_{\mathrm{nec}}.
\]

继续定义：

\[
S_5
=
\alpha_1C_{\mathrm{sem}}
+
\alpha_2C_{\mathrm{cov}}
+
\alpha_3C_{\mathrm{trust}}
+
\alpha_4C_{\mathrm{jb}}
+
\alpha_5C_{\mathrm{nec}},
\]

其中：

\[
\sum_{i=1}^{5}\alpha_i=1.
\]

但它的职责必须重新写。

### 语义清晰度

它处理：

\[
\text{“到底想做什么”}
\]

这一类问题。

例如：

“帮我把那边开一下。”

对象不明确，语义置信度低。

这不属于硬规则违反。

### 证据覆盖度

它处理：

\[
\text{非硬证据的支撑充分程度}.
\]

非常重要：硬规则的

\[
\mathcal E_i^H
\]

已经在安全门阶段完成严格检查。

所以这里的覆盖度不要再把所有硬证据重新计算一遍。

可以定义：

\[
\mathcal E_{\mathrm{soft}}
=
\mathcal E_{\mathrm{req}}
\setminus
\bigcup_i\mathcal E_i^H.
\]

五维评分中的证据覆盖主要评价这部分。

这样可以减少“一个证据既被硬门罚一次，又在评分里再罚一次”的重复惩罚。

### 证据可信度

处理通过硬门以后剩余证据的综合可信程度。

### 越狱抑制

处理一级、二级语义—事实冲突和上下文诱导。

最高等级的安全规则绕过、系统模式伪造已经进入：

\[
V_{\mathrm{authority}}.
\]

因此：

\[
\text{三级冲突}\rightarrow\text{硬门}
\]

\[
\text{低等级冲突}\rightarrow C_{\mathrm{jb}}
\]

边界会非常清楚。

### 场景必要性

继续保留，但只能在

\[
B_{\mathrm{hard}}=0
\]

且

\[
p_{\mathrm{safe}}
\ge
\theta_{\mathrm{causal}}^{B}
\]

时生效。

你之前的：

\[
\alpha_5
\le
0.1\alpha_4
\]

这个约束可以保留。

而且可以进一步明确：

\[
C_{\mathrm{nec}}
\]

只能改变五维评分，不能改变：

\[
B_{\mathrm{hard}}
\]

和

\[
p_{\mathrm{safe}}
<
\theta_{\mathrm{causal}}^{B}
\]

导致的阻断结果。

于是形成严格优先级：

\[
\boxed{
\text{硬安全规则}
>
\text{因果安全风险}
>
\text{五维综合评分}
>
\text{场景必要性补偿}
}
\]

这套层级在逻辑上会非常稳。

## 怎样通过实验把“两个单独都不够、组合最好”证明出来

这里是这次修改最关键的实验设计。不要只做一个总攻击成功率，然后说组合效果好。

一定要构造**风险分型消融实验**。

建议至少设计四个系统：

| 方案 | 硬规则安全门 | 因果贝叶斯 | 五维评分 |
|---|---:|---:|---:|
| 基础方案 | × | × | ✓ |
| 安全门方案 | ✓ | × | ✓ |
| 因果方案 | × | ✓ | ✓ |
| 完整方案 | ✓ | ✓ | ✓ |

然后把测试集分成五类。

### 明确硬规则违反集

包括：

低能见度行驶关闭必要照明；

车辆运动过程中请求打开受限车门；

明确不满足硬执行状态约束；

安全规则绕过请求；

关键证据篡改；

伪造系统模式。

这类数据中应该出现：

\[
\text{安全门方案}
\gg
\text{因果方案}.
\]

原因很好解释：这些风险具有明确规范边界，不应该依赖概率模型自行推断。

《道路交通安全法实施条例》第五十八条关于低能见度条件下车辆照明使用的规定可以直接作为一个真实法规来源案例，而现行GB 7258—2017可以继续作为车辆运行安全强制性技术要求来源之一。citeturn3search0turn7search0

### 复杂因果风险集

专门构造：

\[
\text{没有任何单条硬规则明确违反},
\]

但多因素组合导致高风险的场景。

例如：

目标侧车辆距离、

相对速度、

本车速度、

道路附着、

侧向可用空间

共同作用于变道后安全状态。

此时预期：

\[
\text{因果方案}
\gg
\text{安全门方案}.
\]

这才真正体现你第二节因果贝叶斯存在的价值。

### 证据攻击集

篡改、

重放、

过期、

关键证据缺失、

跨源矛盾。

这类测试能够验证：

\[
\text{安全门}
\]

在因果网络输入之前阻断错误观测。

### 语义与上下文攻击集

上下文注入、

权限伪造、

安全规则绕过、

动作对象歧义。

这里可以观察安全门与五维评分的协同。

### 正常安全请求集

这个集合特别重要。

用于证明完整方案并没有由于增加安全门和因果贝叶斯导致大量正常指令误拒。

最后不要只报告总准确率。

我建议增加一个非常适合你们的指标：

\[
\mathrm{FNR}_{k}
=
\frac{
\text{第 }k\text{ 类危险请求被错误放行数量}
}{
\text{第 }k\text{ 类危险请求总数}
}.
\]

再定义：

\[
\boxed{
R_{\mathrm{worst}}
=
\max_{k}
\mathrm{FNR}_{k}
}
\]

即**最坏风险类别漏放率**。

这比平均准确率更适合安全系统。

因为安全门方案可能总体很好，却在复杂组合风险类别表现差；因果方案也可能总体很好，却在显式硬规则和证据篡改类别存在漏洞。

完整系统需要表现出：

\[
R_{\mathrm{worst}}^{\mathrm{full}}
<
R_{\mathrm{worst}}^{\mathrm{gate}}
\]

以及

\[
R_{\mathrm{worst}}^{\mathrm{full}}
<
R_{\mathrm{worst}}^{\mathrm{causal}}.
\]

还可以定义一个非常漂亮的**协同增益指标**：

\[
G_{\mathrm{syn}}
=
\min
\left(
\mathrm{FNR}_{\mathrm{gate}},
\mathrm{FNR}_{\mathrm{causal}}
\right)
-
\mathrm{FNR}_{\mathrm{full}}.
\]

若

\[
G_{\mathrm{syn}}>0,
\]

说明组合方案优于表现更好的单模块方案。

更高级一点，可以按危险严重等级赋权：

\[
L_{\mathrm{risk}}
=
\sum_{k}
w_k
\mathrm{FNR}_{k}
+
\lambda
\mathrm{FPR},
\]

其中：

\[
w_{\mathrm{hard}}
>
w_{\mathrm{causal}}
>
w_{\mathrm{normal}}.
\]

这样你们优化的目标就会更符合安全系统，而不是简单追求分类准确率。

还建议加两个很有说服力的故障注入实验。

第一个：

> 人为删除若干硬规则，但保持因果模型完整。

观察完整系统仍然能够通过因果推理拦截一部分组合风险。

第二个：

> 人为扰动部分因果网络参数或移除部分因果边，但保持硬规则安全门完整。

观察明确法规红线仍保持接近确定性的阻断能力。

这两个实验能直接说明：

\[
\boxed{
\text{两条防线具有风险异质性和一定故障隔离能力}
}
\]

而且会比单纯做一个“有安全门/无安全门”的消融高级很多。

## 新第三节建议直接这样重写

我建议第二章保持现在的大结构，第二节的三个核心算法基本保留，尤其是当前的“面向执行后安全状态的因果贝叶斯推理算法”不要删。第三节整体换成下面的结构。

### 建议的新节标题

```latex
\subsection{可验证硬规则安全门及其与因果贝叶斯的协同裁决机制}
```

我更推荐这个标题。

因为“硬规则安全门”被明确抬升成一个单独关键技术，“因果贝叶斯”又天然和第二节形成衔接。

### 第一小节

```latex
\subsubsection{多源安全规范驱动的硬规则构建与可信发布}
```

这里写：

法规和标准来源体系；

可信安全知识到硬规则的晋升条件；

法规条款原子化；

受约束大模型离线抽取；

条件—动作—执行后状态结构化；

硬规则资格筛选；

证据依赖绑定；

数据库和知识图谱表示；

规则冲突形式验证；

人工审核；

规则签名和版本发布。

核心创新公式：

\[
\mathcal K_{\mathrm{trusted}}
\xrightarrow{\mathrm{HardEligible}}
\mathcal K_{\mathrm{hard}}.
\]

这就是整个安全门“从哪里来”的答案。

国内硬规则来源可以以道路交通安全法律法规、现行强制性车辆安全标准为第一层；功能安全和网络安全相关国家标准提供工程约束和安全治理依据。国际部署时再根据车辆市场适用联合国车辆法规和国际标准。联合国第48号法规覆盖车辆照明与光信号装置的安装，第11号法规涉及车门锁止和车门保持部件，可用于国际车型相关规则源的适用性分析。citeturn8search6turn8search1

### 第二小节

```latex
\subsubsection{面向执行后安全不变量的确定性安全门控算法}
```

这里才正式写安全门。

重点：

\[
\widehat{\mathbf s}^{+}
=
T_{\mathrm{act}}(\mathbf s,a)
\]

以及：

\[
V_i
=
\mathrm{Active}_i
\land
\neg\phi_i(\widehat{\mathbf s}^{+}).
\]

安全门：

\[
B_{\mathrm{hard}}
=
V_{\mathrm{rule}}
\lor
V_{\mathrm{mandatory}}
\lor
V_{\mathrm{integrity}}
\lor
V_{\mathrm{authority}}.
\]

明确写：

\[
B_{\mathrm{hard}}=1
\Rightarrow
\mathrm{BLOCK}.
\]

同时强调安全门自身：

**没有因果贝叶斯概率。**

**没有五维评分。**

**没有大模型动态生成规则。**

这才能叫独立安全门。

### 第三小节

```latex
\subsubsection{硬规则安全门与因果贝叶斯的非对称协同裁决}
```

这一节专门讲：

安全门覆盖

\[
\Omega_H;
\]

因果贝叶斯覆盖

\[
\Omega_C;
\]

且：

\[
\Omega_H
\cap
\Omega_C
\neq
\varnothing,
\]

但：

\[
\Omega_H
\not\subseteq
\Omega_C,
\qquad
\Omega_C
\not\subseteq
\Omega_H.
\]

完整系统覆盖：

\[
\Omega_{\mathrm{full}}
=
\Omega_H
\cup
\Omega_C.
\]

这会成为理论上解释“为什么两个都要”的最简单表达。

然后放最终裁决公式：

\[
D=
\begin{cases}
\mathrm{BLOCK}, & B_{\mathrm{hard}}=1,\\
\mathrm{BLOCK}, & p_{\mathrm{safe}}<\theta_{\mathrm{causal}}^B,\\
\mathrm{REVIEW}, &
\theta_{\mathrm{causal}}^B
\leq
p_{\mathrm{safe}}
<
\theta_{\mathrm{causal}}^P,\\
D_5, &
p_{\mathrm{safe}}\geq\theta_{\mathrm{causal}}^P.
\end{cases}
\]

再放：

\[
S_5
=
\sum_i\alpha_i C_i.
\]

如此三者彻底分层。

### 第四小节

```latex
\subsubsection{安全门与因果贝叶斯互补性的消融验证}
```

实验直接做：

\[
\text{五维评分}
\]

\[
\text{硬规则安全门+五维评分}
\]

\[
\text{因果贝叶斯+五维评分}
\]

\[
\text{硬规则安全门+因果贝叶斯+五维评分}.
\]

预期结果图绝对不要只画总准确率。

我推荐一张非常有冲击力的分组柱状图：

横轴：

硬规则风险、因果组合风险、证据攻击、上下文攻击、正常请求；

纵轴：

危险放行率。

理想形态应该是：

**安全门单独方案：硬规则风险柱很低，复杂因果风险明显较高；**

**因果贝叶斯单独方案：复杂因果风险柱很低，硬规则/证据攻击仍存在残余；**

**完整方案：所有危险类别都低，同时正常误拒率保持受控。**

这一张图就能把你现在最想表达的观点直接说清楚。

最后，再加一个雷达图或者风险覆盖图，把四组方案的：

硬红线覆盖、

复杂组合风险识别、

证据攻击防御、

正常请求可用性、

可解释性

放在一起。

这样评委很容易得出：

\[
\boxed{
\text{安全门负责守住“绝对不能错”的边界}
}
\]

\[
\boxed{
\text{因果贝叶斯负责补足“规则没有写完”的风险空间}
}
\]

\[
\boxed{
\text{五维评分负责处理“仍然无法完全确定”的剩余区域}
}
\]

最终整个系统的安全逻辑可以浓缩成一句非常适合放在新第三节开头的话：

> **本方案采用“确定性硬边界—因果风险包络—柔性可信评分”三级安全裁决结构。硬规则安全门根据法规、强制标准及审核安全不变量对不可补偿风险执行确定性阻断；因果贝叶斯进一步推理未触发显式硬规则情况下多源物理因素耦合产生的执行后风险；对于同时通过两级安全约束的请求，再利用五维安全评分处理语义、证据与上下文中的剩余不确定性。由此，硬规则的确定性保证、因果模型的复杂风险泛化能力和动态评分的可用性调节能力形成互补。**

这个版本会比当前第三节的“统一安防框架”强很多。因为原第三节在横向扩展攻击面，新方案在纵向深入一个真正能讲出**规则来源、规则形式化、数据库组织、形式验证、执行算法、能力边界、因果互补和消融证明**完整技术闭环的关键技术。

而且它和你当前第二节几乎可以无缝衔接：第二节继续负责

\[
\boxed{
\text{安全知识召回}
\rightarrow
\text{证据图}
\rightarrow
\text{因果贝叶斯}
}
\]

第三节负责

\[
\boxed{
\text{可信安全知识}
\rightarrow
\text{硬规则晋升}
\rightarrow
\text{可验证安全门}
\rightarrow
\text{与因果贝叶斯协同}
\rightarrow
\text{五维最终裁决}
}
\]

这样第二章最终会形成两项非常鲜明的关键技术：**第二节解决“如何理解安全条件并推理复杂风险”，第三节解决“如何建立任何智能推理都无法越过的确定性安全底线”，二者最终在裁决端形成互补闭环。**