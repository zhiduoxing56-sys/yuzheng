# intent_anchor_set_v1_3 修订说明

## 修订原则

- 基线：v1.2 全量留一正式意图 Top1 = 95.23%，P0 = 1，P1 = 45。
- 本轮只处理 v1.2 当前全部 46 条 P0/P1 正式意图失败锚点。
- P2/P3/P4 完全不动；驾驶模式旁路完全不动；安全注入完全不动。
- 不新增、不删除锚点，总量保持不变。
- 每条修改均保持原 Intent 语义边界，只增强动作方向、对象或控制属性的可区分性。

## 文件摘要

- v1.2 SHA256：`CB54A33A6DC95EC5D02BF648D621AE80303E926A65DEB3DE22E72A612C553DF8`
- v1.3 SHA256：`B88B4D9DCC9CDFDB27EC6D25038AF6E7E5D3F01FE6937BA4777815987FB65BFF`
- 总锚点：1466 → 1466
- 正式意图锚点：1426 → 1426
- 修改：46 条；新增：0；删除：0

## 逐项修改

### MIRROR_HEATING_OFF

关闭侧两条失败样本被开启侧吸走；保留“停止/关闭”动作，同时补全“外后视镜/除霜功能”对象，使其更贴近本意图现有关闭锚点。

- `停止后视镜加热` → `停止外后视镜加热`；v1.2 错误 Top1：`MIRROR_HEATING_ON`；优先级：P1。
- `后视镜除霜关闭` → `关闭后视镜除霜功能`；v1.2 错误 Top1：`MIRROR_HEATING_ON`；优先级：P1。

### DEFROST_ON

开启侧与关闭侧存在强对称短句，留一后容易被反向动作吸走；增加前后风挡、后挡、自动除霜等对象/模式信息，减少仅靠“除霜/除雾”公共词竞争。

- `开启风挡除霜除雾` → `开启前后风挡除霜除雾`；v1.2 错误 Top1：`DEFROST_OFF`；优先级：P1。
- `打开后挡除雾` → `开启后挡除雾功能`；v1.2 错误 Top1：`DEFROST_OFF`；优先级：P1。
- `把除霜除雾功能打开` → `打开自动除霜除雾`；v1.2 错误 Top1：`DEFROST_OFF`；优先级：P1。

### DEFROST_OFF

关闭侧与开启侧存在强对称短句；将动作写成关闭/停止并补足前后风挡、功能或自动除霜限定，增强关闭簇内部近邻。

- `关闭风挡除霜除雾` → `关闭前后风挡除霜除雾`；v1.2 错误 Top1：`DEFROST_ON`；优先级：P1。
- `关掉挡风玻璃除霜` → `关闭前挡风玻璃除霜功能`；v1.2 错误 Top1：`DEFROST_ON`；优先级：P1。
- `停止前挡除雾` → `停止前挡风除雾功能`；v1.2 错误 Top1：`DEFROST_ON`；优先级：P1。
- `把除霜功能切换成关闭状态` → `关闭自动除霜除雾功能`；v1.2 错误 Top1：`DEFROST_ON`；优先级：P1。

### WINDSHIELD_HEATING_ON

原句与“关闭后挡风玻璃加热”高度镜像；改为“开启…加热功能”，靠近本意图已有开启锚点并保留后挡对象。

- `启动后挡风玻璃加热` → `开启后挡风玻璃加热功能`；v1.2 错误 Top1：`WINDSHIELD_HEATING_OFF`；优先级：P1。

### ESC_ENABLE

启用/停用两侧在“车辆电子稳定系统”长串上高度重合；显式加入ESC并使用激活/重新启用，增强开启方向。

- `激活车辆电子稳定系统` → `激活ESC电子稳定系统`；v1.2 错误 Top1：`ESC_DISABLE`；优先级：P1。
- `恢复车身稳定控制功能` → `重新启用ESC车身稳定控制`；v1.2 错误 Top1：`ESC_DISABLE`；优先级：P1。

### ESC_DISABLE

启用/停用两侧在长对象词上高度重合；显式加入ESC并固定禁用/停用/关闭方向，增强停用方向。

- `禁用车辆电子稳定系统` → `禁用ESC电子稳定系统`；v1.2 错误 Top1：`ESC_ENABLE`；优先级：P1。
- `停用车身稳定系统` → `停用ESC车身稳定系统`；v1.2 错误 Top1：`ESC_ENABLE`；优先级：P1。
- `关闭车身稳定系统` → `关闭ESC车身稳定系统`；v1.2 错误 Top1：`ESC_ENABLE`；优先级：P1。

### TRUNK_OPEN

原句与关闭侧“关闭车辆行李厢”形成近乎镜像；改为“后备箱盖+打开”，直接靠近本意图“打开后备箱盖”。

- `开启车辆行李厢` → `把后备箱盖打开`；v1.2 错误 Top1：`TRUNK_CLOSE`；优先级：P1。

### TRUNK_CLOSE

三条关闭表达分别被打开/锁定吸走；统一改为“合上/关严+后备箱盖/后备厢”，避开“打开”和“上锁”词簇。

- `关闭车辆行李厢` → `把后备箱盖合上`；v1.2 错误 Top1：`TRUNK_OPEN`；优先级：P1。
- `把后备箱闭合关闭` → `合上后备箱盖`；v1.2 错误 Top1：`TRUNK_OPEN`；优先级：P1。
- `关上后备厢` → `把后备厢关严`；v1.2 错误 Top1：`TRUNK_LOCK`；优先级：P1。

### TRUNK_SET_POSITION

开度意图应突出开度/高度/百分比调节。当前P0及两条P1被打开/锁定吸走，因此改为明确的“开度/开启高度+调节”表达。

- `设置行李厢开启幅度百分比` → `把后备箱开度调到50%`；v1.2 错误 Top1：`TRUNK_LOCK`；优先级：P0。
- `后备箱调太低了` → `把后备箱开启高度调高一点`；v1.2 错误 Top1：`TRUNK_OPEN`；优先级：P1。
- `把后备箱开启高度调低一点` → `把后备箱开度调低一点`；v1.2 错误 Top1：`TRUNK_OPEN`；优先级：P1。

### TRUNK_LOCK

原句被解锁侧吸走；将“上锁”置于明确的行李厢对象后，靠近本意图“锁上后备箱/锁定后备厢”。

- `给行李厢上锁` → `把行李厢锁上`；v1.2 错误 Top1：`TRUNK_UNLOCK`；优先级：P1。

### TRUNK_UNLOCK

“尾门”单独出现易与车门解锁混淆，“把后备厢解锁”又受关闭侧拼音影响；补全后备箱对象并改成更接近本意图解锁簇的表述。

- `解除尾门锁定` → `把后备箱尾门解锁`；v1.2 错误 Top1：`DOOR_UNLOCK`；优先级：P1。
- `把后备厢解锁` → `把后备厢解锁一下`；v1.2 错误 Top1：`TRUNK_CLOSE`；优先级：P1。

### HOOD_OPEN

打开/关闭发动机舱盖形成镜像混淆；开启侧改用“掀开”，与本意图已有“掀开引擎盖”形成同向语义簇。

- `打开发动机舱盖` → `掀开发动机舱盖`；v1.2 错误 Top1：`HOOD_CLOSE`；优先级：P1。
- `把前舱盖打开` → `把前舱盖掀开`；v1.2 错误 Top1：`HOOD_CLOSE`；优先级：P1。

### HOOD_CLOSE

打开/关闭发动机舱盖形成镜像混淆；关闭侧改用“合上”，与本意图已有“合上引擎盖”形成同向语义簇。

- `关闭发动机舱盖` → `合上发动机舱盖`；v1.2 错误 Top1：`HOOD_OPEN`；优先级：P1。
- `把前舱盖关上` → `把前舱盖合上`；v1.2 错误 Top1：`HOOD_OPEN`；优先级：P1。

### SUNROOF_CLOSE

“完全关闭”被“全开”类锚点吸走；改成“彻底关上”，保留全关闭语义并贴近“把天窗关上”。

- `把天窗完全关闭` → `把天窗彻底关上`；v1.2 错误 Top1：`SUNROOF_OPEN`；优先级：P1。

### CRUISE_DISABLE

“取消当前巡航控制”被开启巡航吸走；恢复更简洁且与“退出巡航模式/关闭巡航模式”同簇的“取消巡航模式”。

- `取消当前巡航控制` → `取消巡航模式`；v1.2 错误 Top1：`CRUISE_ENABLE`；优先级：P1。

### LOW_BEAM_ON

近光与远光的“车辆…照明”表达高度重合；改为显式“近光灯”，强化目标对象。

- `开启车辆近光照明` → `开启车辆近光灯`；v1.2 错误 Top1：`LOW_BEAM_OFF`；优先级：P1。

### LOW_BEAM_OFF

关闭近光同时受到远光关闭和近光开启干扰；统一显式“近光灯”对象，并用关闭/停用/关掉强化方向。

- `关闭车辆近光照明` → `关闭车辆近光灯`；v1.2 错误 Top1：`HIGH_BEAM_OFF`；优先级：P1。
- `取消近光照明` → `停用车辆近光灯`；v1.2 错误 Top1：`HIGH_BEAM_OFF`；优先级：P1。
- `近光灯关掉` → `把近光灯关掉`；v1.2 错误 Top1：`LOW_BEAM_ON`；优先级：P1。

### HIGH_BEAM_ON

近远光开启表达高度重合；改为显式“远光灯”，强化对象区别。

- `启用车辆远光照明` → `启用车辆远光灯`；v1.2 错误 Top1：`LOW_BEAM_ON`；优先级：P1。

### HIGH_BEAM_OFF

近远光关闭表达高度重合；改为显式“远光灯”，并保留关闭/停用方向。

- `关闭车辆远光照明` → `关闭车辆远光灯`；v1.2 错误 Top1：`LOW_BEAM_OFF`；优先级：P1。
- `取消远光照明` → `停用车辆远光灯`；v1.2 错误 Top1：`LOW_BEAM_OFF`；优先级：P1。

### FOG_LIGHT_OFF

原长句在拼音通道被开启雾灯吸走；压缩成明确对象+关闭动作的“关闭后雾灯开关”。

- `找到后雾灯开关设置并关闭` → `关闭后雾灯开关`；v1.2 错误 Top1：`FOG_LIGHT_ON`；优先级：P1。

### WINDOW_CLOSE

“所有车窗全部关闭”与开启侧“所有车窗全部打开”高度镜像；改用“全车车窗全部关上”靠近关闭簇。

- `所有车窗全部关闭` → `全车车窗全部关上`；v1.2 错误 Top1：`WINDOW_OPEN`；优先级：P1。

### WINDOW_SET_POSITION

两条位置样本分别被车门开度与车窗打开吸走；显式加入“车窗开度+调到百分比”，强化位置调节属性。

- `右后车窗开到50%` → `右后车窗开度调到50%`；v1.2 错误 Top1：`DOOR_SET_POSITION`；优先级：P1。
- `把车窗开启幅度调到70%` → `把车窗开度调到70%`；v1.2 错误 Top1：`WINDOW_OPEN`；优先级：P1。

### DOOR_CLOSE

“电动侧门”对象不够稳定，被车窗/尾门关闭吸走；明确为“电动侧滑门”。

- `关闭车辆电动侧门` → `关闭车辆电动侧滑门`；v1.2 错误 Top1：`WINDOW_CLOSE`；优先级：P1。

### DOOR_SET_POSITION

车门与车窗位置表达高度相似；保留左后车门对象，并将核心控制属性统一为“车门开度”。

- `把左后车门开启幅度调到50%` → `把左后车门开度调到50%`；v1.2 错误 Top1：`WINDOW_SET_POSITION`；优先级：P1。

### DOOR_UNLOCK

“解除…锁定”更容易靠近锁定簇；改为直接的“把左前车门解锁”，贴近本意图大量解锁锚点。

- `解除左前车门锁定` → `把左前车门解锁`；v1.2 错误 Top1：`DOOR_LOCK`；优先级：P1。

### PARKING_BRAKE_RELEASE

原句同时含“手刹/制动”，容易被施加驻车制动吸走；保留最直接的释放表达“松开手刹”。

- `松开手刹释放制动` → `松开手刹`；v1.2 错误 Top1：`PARKING_BRAKE_APPLY`；优先级：P1。

## 静态检查

- YAML 可正常解析：通过
- 71 个正式意图保留：通过
- 驾驶模式旁路未修改：通过
- 安全注入未修改：通过
- 跨目标完全重复锚点：0
- 空字符串：0
- 总锚点数量变化：0

## 下一步

使用与 v1.2 完全一致的留一诊断脚本、BGE 模型、字面/拼音算法、RRF 参数和阈值，对 v1.3 做一次冻结验证。验证后不要自动继续修改。
